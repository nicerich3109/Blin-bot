🥞 Blin-bot — Discord-бот для вступления и отпусков

Blin-bot управляет заявками на вступление и заявками на отпуск для
роплей-сообщества на двух под-серверах (**Denver** и **Phoenix**).
Построен на **discord.py**, все данные хранятся в `data.json`.

![Python](https://img.shields.io/badge/Python-3.10%2B-3776AB?logo=python&logoColor=white)
![discord.py](https://img.shields.io/badge/discord.py-2.4%2B-5865F2?logo=discord&logoColor=white)
![Servers](https://img.shields.io/badge/Servers-Denver%20%7C%20Phoenix-orange)
![Storage](https://img.shields.io/badge/Storage-JSON-lightgrey)
![License](https://img.shields.io/badge/License-Educational%20Use-brightgreen)

---

## 📌 Оглавление

- [✨ Основные возможности](#-основные-возможности)
- [🚀 Функции](#-функции)
- [⚙️ Как это работает](#️-как-это-работает)
- [🧩 Технологии](#-технологии)
- [📁 Структура проекта](#-структура-проекта)
- [⚡ Быстрый старт](#-быстрый-старт)
- [🧠 Допущения по ТЗ](#-допущения-по-неоднозначным-местам-тз)
- [🕘 История изменений](#-история-изменений)
- [📄 Лицензия](#-лицензия)

---

## ✨ Основные возможности

- 🖥️ Все действия — через кнопки и слэш-команды, без ручного редактирования ролей
- 🎫 Приватные тикет-каналы для каждой заявки на вступление
- 📞 Обзвон заявителя прямо из тикета, с автоматической выдачей роли доступа к каналу обзвона
- 🌴 Заявки на отпуск с точным временем окончания и автоснятием роли по расписанию
- ⏳ Кулдаун на подачу заявок — защита от спама
- 🪵 Подробное логирование всех действий и ошибок в `bot.log`

---

## 🚀 Функции

### Заявки на вступление
- Кнопки **Denver** / **Phoenix** → модалка (никнейм, статик, OOC возраст)
- Создание приватного тикет-канала `DN-001` / `PHX-001` с доступом заявителю и рекрутёрам
- В тикете сразу доступны три кнопки: **Принять**, **Обзвон**, **Отклонить**
- **Обзвон**: выбор одного из двух голосовых каналов сервера → заявителю выдаётся
  роль обзвона (открывает видимость канала) и приходит уведомление, в какой канал зайти
- При принятии: смена ника на `New DN/PHX Имя`, выдача роли нового участника, роль
  обзвона (если была) автоматически снимается
- Обработка кнопками или командами `/принять номер` / `/отклонить номер причина`
- ⏳ Кулдаун 2 минуты между подачами заявки на одного пользователя

### Заявки на отпуск
- Модалка: ID дискорда, дата и время окончания (шаг 10 минут), причина
- Сервер определяется каналом подачи — отдельного поля не нужно
- Обработка кнопками **Принять** / **Отклонить** или командами
- Автоматическая выдача роли отпуска и обновление списка «сейчас в отпуске»
- Фоновая проверка каждые 15 минут снимает роль по истечении срока или при выходе
  участника с сервера
- Команда `/вынесение_из_отпуска участник причина` — досрочный принудительный вывод

---

## ⚙️ Как это работает
Заявитель Бот Рекрутёр
│ │ │
│──── Denver/Phoenix ─▶│ │
│◀────── модалка ─────│ │
│──── никнейм и т.д. ─▶│ │
│ │──── создаёт тикет-канал ──────▶│
│ │ │──▶ Обзвон (выбор канала)
│◀── роль обзвона + уведомление о канале ─────────────────│
│ │ │──▶ Принять / Отклонить
│◀── новый ник / роль или причина отказа ──────────────────│
│ │──── удаляет тикет-канал ───────│
---

## 🧩 Технологии

| Технология | Назначение |
|---|---|
| **Python 3.10+** | язык реализации |
| **discord.py 2.4+** | взаимодействие с Discord API, слэш-команды, кнопки, модалки |
| **JSON (`data.json`)** | персистентное хранилище заявок, отпусков и счётчиков |
| **logging + RotatingFileHandler** | логирование в консоль и `bot.log` с ротацией |
| **asyncio** | фоновая проверка окончания отпусков |

---

## 📁 Структура проекта

| Файл | Назначение |
|---|---|
| `config.py` | Все ID каналов/разделов/ролей, токен, таймауты и кулдауны |
| `logger_setup.py` | Настройка логирования в консоль и в файл `bot.log` |
| `storage.py` | Хранение данных в `data.json` (заявки, отпуска, счётчики, кулдауны) |
| `utils.py` | Парсинг даты/времени, права доступа, безопасный поиск участника, персистентные сообщения |
| `decisions.py` | Общая логика принятия/отклонения заявок (вступление и отпуск) |
| `ui_decision.py` | Кнопки «Принять» / «Обзвон» / «Отклонить» и модалка причины отказа |
| `applications.py` | Заявки на вступление: кнопки Denver/Phoenix, модалка, тикет-канал, обзвон |
| `vacations.py` | Заявки на отпуск: модалка, список отпускников, снятие роли по сроку |
| `commands.py` | Слэш-команды `/принять`, `/отклонить`, `/вынесение_из_отпуска` |
| `bot.py` | Точка входа — запускать нужно именно этот файл |

---

## ⚡ Быстрый старт

### Установка

```bash
python3 -m venv venv
source venv/bin/activate      # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### Настройка

1. В Discord Developer Portal у приложения бота включите **Privileged
   Gateway Intents → Server Members Intent**.
2. Выдайте боту права: `Manage Roles`, `Manage Channels`,
   `Manage Nicknames`, `View Channels`, `Send Messages`,
   `Embed Links`, `Read Message History`.
3. **Важно:** роль бота должна стоять **выше** ролей рекрутёров, новых
   участников, ролей отпуска и ролей обзвона в списке ролей сервера —
   иначе бот не сможет их выдавать/снимать и менять ники таким людям.
4. Задайте токен бота:
```bash
   export DISCORD_TOKEN="ваш_токен"
```
5. При необходимости укажите `GUILD_ID` в `config.py` — тогда
   слэш-команды появятся на сервере мгновенно после запуска (иначе
   обновление может занять до часа).
6. Проверьте, что все ID в `config.py` (каналы, роли, в том числе роли
   и каналы обзвона) совпадают с реальными ID на вашем сервере
   (Настройки → Дополнительно → Режим разработчика → ПКМ по
   каналу/роли → «Копировать ID»).

### Запуск

```bash
python3 bot.py
```

Логи пишутся одновременно в консоль и в файл `bot.log` (с ротацией —
старые логи архивируются, файл не растёт бесконечно). При проблемах с
выдачей ролей или другими действиями бота в первую очередь смотрите
именно `bot.log` — там указана точная причина (участник не найден,
роль не найдена, у бота нет прав и т.п.).

При старте бот сам разместит (или обновит, если уже есть) сообщения с
кнопками в канале заявок на вступление и в обоих каналах отпуска
(Denver и Phoenix).

---

## 🧠 Допущения по неоднозначным местам ТЗ

- Поле «время окончания отпуска» трактуется как точное время суток на
  дату окончания (то есть дата + время = точный момент, когда роль
  будет снята), а не как общая продолжительность отпуска в часах.
- Право принимать решения по заявкам на отпуск дано тем же
  рекрутёрам/чиф-рекрутёрам, что и по заявкам на вступление
  (`config.VACATION_STAFF_ROLES`) — в ТЗ конкретная роль не была
  указана. Список легко расширить нужной ролью.
- Интервал фоновой проверки отпусков увеличен с предложенных 10 до 15
  минут — обоснование см. в комментарии рядом с
  `VACATION_CHECK_INTERVAL_MINUTES` в `config.py`.
- Роль обзвона привязана к серверу (DN/PHX), а не к конкретному из
  двух его каналов — выбор канала определяет, куда именно позвать
  заявителя, а видимость самих каналов обзвона для этой роли
  настраивается правами канала в Discord.

---

## 🕘 История изменений
### v1.3
Phoenix → при одобрении выдаётся роль 1543543788939509791.
Denver → при одобрении выдаётся роль 1543544272559808573.
После обработки заявки канал больше не удаляется.
Phoenix → канал переносится в архив 1543674852676735128.
Denver → канал переносится в архив 1543674967881941093.
Заявителю выставляется view_channel=False, send_messages=False, read_message_history=False — то есть он полностью теряет доступ к заявке.
Существующая роль нового участника и остальные функции бота сохранены.
Из старой логики автоматического удаления тикета убрано удаление.

**Добавлено**
- Кнопка **«Обзвон»** на заявках вступления: выбор одного из двух
  голосовых каналов сервера (DN/PHX), выдача заявителю роли обзвона и
  уведомление в тикете с указанием канала.
- Кулдаун **2 минуты** на подачу заявки на вступление — защищает от
  спама повторными заявками.

**Изменено**
- При принятии заявки ник теперь выдаётся без префикса «Blin»:
  `New DN/PHX Имя` вместо `New Blin DN/PHX Имя`.
- Роль обзвона автоматически снимается при вынесении решения по
  заявке (принята она или отклонена).

### v1.2

**Добавлено**
- Кнопка **«Обзвон»** на заявках вступления: выбор одного из двух
  голосовых каналов сервера (DN/PHX), выдача заявителю роли обзвона и
  уведомление в тикете с указанием канала.
- Кулдаун **2 минуты** на подачу заявки на вступление — защищает от
  спама повторными заявками.

**Изменено**
- При принятии заявки ник теперь выдаётся без префикса «Blin»:
  `New DN/PHX Имя` вместо `New Blin DN/PHX Имя`.
- Роль обзвона автоматически снимается при вынесении решения по
  заявке (принята она или отклонена).

### v1.1.2

**Исправлено**
- Проблема с иерархией ролей бота (не удаётся выдать роль или сменить
  ник) логируется в `bot.log` и показывается ephemeral-сообщением
  тому, кто обработал заявку.
- Тикет-канал заявки на вступление теперь автоматически удаляется
  через `config.TICKET_DELETE_DELAY_SECONDS` секунд после решения —
  раньше он оставался навсегда.
- Участник, покинувший сервер до окончания отпуска, больше не
  «зависает» в списке «сейчас в отпуске» — запись закрывается сразу
  же, как только его не стало на сервере.

**Изменено**
- Шаг времени окончания отпуска уменьшен с 30 до 10 минут, фоновая
  проверка окончания отпусков — тоже раз в 10 минут.

**Добавлено**
- Проверка существования и присутствия на сервере указанного
  участника перед созданием заявки на отпуск.
- Команда `/вынесение_из_отпуска участник причина` для принудительного
  досрочного вывода из отпуска.

### v1.1.1

**Добавлено**
- Выбор сервера в заявке на вступление через кнопки **Denver** /
  **Phoenix** вместо текстового поля.
- Кнопки **Принять** / **Отклонить** прямо в заявках, в дополнение к
  командам.
- Модальное окно «Причина» при отказе.
- Команды `/принять` и `/отклонить`, работающие с обоими типами
  заявок автоматически.
- Поле «Время окончания» в заявке на отпуск с шагом 10 минут.
- Файл `bot.log` с подробным логированием.
- Код разложен по отдельным файлам вместо одного `bot.py`.

**Исправлено**
- Баг с невыдачей роли отпуска при одобрении заявки (не искался
  участник через API при промахе локального кэша).

**Убрано**
- Поля «Планы после вступления» и «Откуда узнали о нас» из заявки на
  вступление.
- Поле «Сервер» из заявки на отпуск (теперь определяется каналом).
- Команды `/принял`/`/отклонил` переименованы в `/принять`/`/отклонить`.

### v1.0
- Заявки на вступление и на отпуск в базовом виде, весь код в одном
  файле `bot.py`.

---

## 📄 Лицензия

Проект создан в учебных целях для внутреннего использования на
Majestic-RP серверах Denver и Phoenix.
